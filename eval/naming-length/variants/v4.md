## Naming

`s.Test(...)` names are business actions/outcomes at every level — not function names, HTTP codes, assertion verbs (`is nil`, `equals`), or data-shape words (`empty slice`). Propose domain replacements. See [naming.md](naming.md).
